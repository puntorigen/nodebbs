{
  "name": "nodebbs",
  "version": "2.0.0",
  "description": "Dial into an ANSI BBS from your terminal. Server streams raw ANSI over WebSocket; the client is a thin modem that plays dial-up sound and throttles to a baud rate.",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "server": "npm --prefix server start",
    "call": "npm --prefix client start"
  },
  "keywords": [
    "bbs",
    "ansi",
    "websocket",
    "modem",
    "terminal",
    "retro"
  ],
  "author": "Pablo Schaffner",
  "license": "ISC"
}
